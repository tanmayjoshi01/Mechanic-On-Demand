package com.mechanicondemand.model;

/**
 * Enum for Subscription Types
 * Different pricing models for booking mechanics
 */
public enum SubscriptionType {
    HOURLY,     // Pay per hour
    MONTHLY,    // Monthly subscription
    YEARLY      // Yearly subscription
}
